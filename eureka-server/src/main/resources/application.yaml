server:
  port: 8761

spring:
  application:
    name: eureka-server

  cloud:
    config:
      enabled: false
      import-check:
        enabled: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
